# viralTier.ai Web App — Local Environment Variables
# Copy .env.example from the root and fill in your values

NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Required for Convex Auth middleware (server-side)
CONVEX_AUTH_ADAPTER_SECRET=your-convex-auth-adapter-secret
